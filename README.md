# Qubic History

This repository stores public weekly Qubic history snapshots generated by Qools.

## Custom Events

Manually curated ecosystem events live in `custom-events/`.

These records are for chart context only. They are not used to claim causality between events and price movement.
